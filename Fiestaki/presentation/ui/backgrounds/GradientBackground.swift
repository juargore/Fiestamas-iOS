//
//  GradientBackground.swift
//  Fiestaki
//
//  Created by Arturo Gomez on 11/9/23.
//

import SwiftUI

/*struct GradientBackground<Content: View>: View {
    
    @ObservedObject var viewModel: AuthViewModel

    init(viewModel: AuthViewModel = AuthViewModel() ) {
        self.viewModel = viewModel
    }
    
    let validateOfflineMode: Bool
    let isStatusBarForDetails: Bool
    let showBackButton: Bool
    let showLogoFiestakiTop: Bool
    let showLogoFiestakiBottom: Bool
    let showUserName: Bool
    let endButton: Int?
    let notificationsCounter: String?
    let showCalendarButton: Bool
    let service: Service?
    let user: FirebaseUserDb?
    let titleScreen: String?
    let addBottomPadding: Bool
    let rating: Int?
    let content: () -> Content
    let onBackButtonClicked: (() -> Void)?
    let onTitleScreenClicked: (() -> Void)?
    let onEndButtonClicked: (() -> Void)?
    let onCalendarButtonClicked: (() -> Void)?
    let onHeartButtonClicked: (() -> Void)?
    let onShareButtonClicked: (() -> Void)?
    let onNavigateAuthClicked: (() -> Void)?

    //var body: some View {
        // Your SwiftUI layout here
        // Use the provided properties and functions to build your UI
        //content().environmentObject(authViewModel)
    //}
}*/

/*struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        GradientBackground {
            Text("Hello, SwiftUI!")
        }
        .environmentObject(AuthViewModel())
    }
}*/
